#!/usr/bin/env bash
# 7-highlander - Responding immediately to SIGTERM

# Define the trap behavior
trap "echo 'I am invincible!!!'" SIGTERM

while true
do
    echo "To infinity and beyond"
    # Run sleep in the background and wait for it
    # This allows the script to process the trap immediately
    sleep 2 &
    wait $!
done
